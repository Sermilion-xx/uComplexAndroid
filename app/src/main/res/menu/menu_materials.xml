<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/my_files_add_file"
        android:title="@string/upload_file"
        android:icon="@drawable/ic_upload_selector"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/my_files_add_folder"
        android:title="@string/add_folder"
        android:icon="@drawable/ic_add_folder_selector"
        app:showAsAction="ifRoom"/>

</menu>